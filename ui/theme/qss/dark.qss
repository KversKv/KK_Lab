/* dark.qss — 全局暗色主题（预留骨架，当前不注入任何全局规则）。

   设计约束（2026-08 视觉重构）：全局 app 级样式**不得**在此铺开，以免波及
   全部页面。Module Test 页面的暗色重构为**页面专属、纯增量叠加**，统一收敛在
   ``qss/module_dark.qss``，经 ``apply_qss(self, "module_dark",
   theme=module_dark_tokens())`` 仅注入该页面根节点，其它页面保持既有主题。

   本文件保留为空骨架，供未来"全应用统一换肤"决策落地时启用；启用前需先
   评审对所有存量页面的影响。 */
