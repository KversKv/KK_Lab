# 项目依赖

# Qt GUI 框架
PySide6>=6.8,<6.10

# 图表绘制
pyqtgraph>=0.13.7,<0.14

# VISA 仪器通信
pyvisa>=1.14,<1.15
pyvisa-py>=0.7.2,<0.8
pyusb>=1.3.1,<2
libusb-package>=1.0.26.3,<2

# mDNS / DNS-SD 仪器发现 (跨子网发现 N6705C 等 LXI 仪器)
zeroconf>=0.130,<1

# 串口通信 (VT6002温箱 / CLK测试)
pyserial>=3.5,<4

# Excel 文件操作
openpyxl>=3.1.5,<4

# 科学计算
numpy==2.4.4

# YAML 配置解析 (Consumption Test - 芯片已保存配置)
PyYAML>=6.0.2,<7

# AI Assist - New API (OpenAI 兼容) HTTP 客户端，原生超时/取消/连接复用
httpx>=0.27,<0.28

# 打包
pyinstaller>=6.0,<7